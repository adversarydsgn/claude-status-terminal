# SYS-235 security-L3 rehearsal fixture

This file exists only to fire the `security-L3` type tag's path trigger
(`**/bws*scope*`) in the PR reviewer's org tag map, so the locked Tier
2 Type B + Thermo escalation path can be observed end to end on a
low-stakes repo. It describes nothing and changes nothing.

Delete this file after SYS-235 Phase 2 calibration closes. The PR that
adds it is never merged.
